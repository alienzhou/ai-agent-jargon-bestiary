# 贡献指南

词条靠「捡」长出来：不是检索术语表，而是你在别处跟 AI 对话时撞见一个不太好理解的词，把**原话连同上下文**丢过来，建成词条。所以每个词都有出处、有现场。

**不做定性评判**：不给分、不判「这词是不是黑话」。只做三件事——翻译成人话、指出它其实是什么、记下在哪撞见的。判断留给读的人。

## 目录

```
.
├── README.md          # 项目门面（定位 + 入口链接）
├── CONTRIBUTING.md     # 本文件（数据规范 + 工程用法）
├── ART.md             # ★ 素材生成规范（风格底座 / prompt / 验收清单）
├── scripts/           # 素材管线与校验
│   ├── style.txt      # ★ 风格底座，全站素材共用的 prompt 前缀
│   ├── gen-art.sh     # 拼 prompt → 交给 $JARGON_IMAGE_CMD（未设则打印，手动出图）
│   ├── gen-term-art.sh# 薄壳：词条专属图，等价于 gen-art.sh term-<slug>
│   ├── opt-art.py     # 压缩（uv run 自带 pillow），opt-art.sh 是兼容入口
│   └── verify-page.js # 页面自检：贴进 DevTools Console 跑，返回 JSON 结论
├── terms/             # 词条数据源，一词一文件
│   └── {slug}.md      # 纯 YAML frontmatter，无正文
├── build.mjs          # 构建脚本
├── dev.mjs            # 开发服务器（监听 terms/ 变更热重载）
├── submit.config.json # 投词入口链接（表单 / GitHub Issue，见「投稿入口」）
├── assets/
│   ├── app.css        # 页面样式（街机卡牌风）
│   ├── app.js         # 页面逻辑（构建时内联进 index.html）
│   └── img/opt/       # 压缩后的 webp/avif，构建时 base64 内联
└── dist/
    ├── lexicon.json   # ★ 结构化数据（对外契约，自建页面用这个）
    └── index.html     # 参考渲染（单文件应用，可直接打开）
```

> 仓库仅提交页面实际使用的压缩素材。生成用 PNG 原图与弃用素材不入库，见 [ART.md](ART.md)。

## 用法

```bash
npm install
npm run build   # 一次性构建
npm run dev     # 开发服务器，改词条自动重编译 + 浏览器刷新
```

## 词条格式

文件名 `terms/{slug}.md`，slug 用小写英文短横线，如 `context-engineering.md`。

**全部内容都在 frontmatter 里，没有 Markdown 正文**——这样每个字段都能被结构化解析。

```markdown
---
term: Context Engineering          # 【必填】词条主名（原文形态）
zh: 上下文工程                      # 中文译名，没有就留空 ""
aliases: [上下文管理, 上下文编排]     # 别名/变体，用于搜索
category: 方法                      # 见下方分类表
kind: 术语                          # 术语 | 代称，见下方词性表（缺省为「术语」）
added: 2026-08-30                  # 收录日期
gloss: 想办法往模型的输入框里塞对的东西。          # 【必填】人话翻译，一句话
actually: Prompt Engineering 的体面版。区别在于…  # 【必填】它其实是什么
origin: 2024-2025 年从 Agent 圈里长出来的…       # 血统：谁先说的、从哪来
note: 一个领域开始给自己的活儿加「工程」后缀…      # 备注/吐槽，可选
usage:                             # 【测验用】谁用对了
  right:
    text: 把上次排查的结论也塞进 prompt，别让它从零猜。   # 唯一用对的那句
    why: 就是在决定给模型看哪些东西。朴素得很。
  wrong:                           # 离谱的误用，至少 3 条（页面随机抽 3）
    - text: 我们招了个 context engineer，专门负责给模型写小作文。
      why: 这不是岗位，这是你每天在做但不肯承认的那件事。
    - text: 上下文工程做完了，模型还是不懂业务，看来得再上一层工程。
      why: 「再上一层工程」是黑话的自我繁殖，观察一下就好，别参与。
decoys:                            # 测验干扰项，恰好 3 条
  - 通过微调让模型记住公司内部术语的过程
  - 把长文档自动切成小块存进向量库的技术
  - 一种根据用户情绪调整回答语气的策略
sightings:                         # 出没记录：在哪撞见的
  - date: 2026-08-30
    source: Claude / 架构讨论
    quote: 我们需要做好 context engineering，把相关文件提前注入。
    note: 这里其实就是「决定给模型看哪些文件」
---
```

字段含 `:` `#` `[` 等 YAML 特殊字符时用双引号包裹。

### 五个内容字段的分工

| 字段 | 回答什么 | 长度 |
|---|---|---|
| `gloss` | 说人话，这词到底指什么 | 一句话，能独立成立 |
| `actually` | 它有没有旧名字 / 是不是真新东西 | 一两句，**必须出现词条本体**（见下） |
| `origin` | 谁造的、从哪来的 | 一两句，可空 |
| `note` | 想补的话（吐槽、观察） | 随意，可空 |
| `usage` | 一句用对的 + 若干句离谱误用，各配一句吐槽 | 见下 |
| `decoys` | 3 条貌似成立的错误释义 | 与 gloss 句式长度接近 |

### 词性（kind）★ 该不该学

和 `category` 正交的第二个维度，回答的是「这词值不值得你花时间搞懂」：

| 值 | 含义 | 例子 |
|---|---|---|
| `术语` | 背后确实有个概念，学了有用。理解它 = 理解一件真事 | 幻觉、Context Engineering、Multi-Agent |
| `代称` | AI 拿它去指代另一个本来就有名字的东西。学了也没用，认出来就行 | 台账（= registry.json）、门禁（= 权限校验）、赋能（= 用） |

判断方法：把这个词换成它指代的那个东西，句子意思有没有损失。
没损失 → `代称`（「AI 赋能业务」= 「业务用了 AI」）；有损失 → `术语`。

页面上，`术语` 是蓝底徽章，`代称` 是纸底徽章，筛选栏第一排可以只看其中一类。
缺省值为 `术语`，所以老词条不写也不会崩。

### 分类（category）

| 值 | 含义 |
|---|---|
| `方法` | 描述一种做法、工程手段 |
| `架构` | 描述系统结构、组件关系 |
| `能力` | 描述模型/系统能做什么 |
| `隐喻` | 借用人类心智或其他领域的词 |
| `营销` | 主要功能是让东西听起来更贵 |
| `失效` | 描述出问题的方式 |

### 用法辨析怎么写（usage）★ 测验主体

测验考的**不是「哪个释义对」，而是「谁用对了这个词」**——给 4 句用到它的话，只有一句用对。

**立场**：这个模式不是真的教学，是讽刺。AI 天天喷黑话，人类反而要去学、去受教育——这件事本身就荒谬。所以选项**不要**做成四个相近释义让人认真辨析，而要做到一看就在瞎搞：

- **`right.text`** —— 一句朴素、正常人会说的话。全场唯一一句人话，平淡无奇，这正是笑点的底。
- **`wrong[].text`** —— **风马牛不相及，越离谱越好**。标准玩法是把词当成完全不同的东西，用一本正经的语气说出来：
  - 当成食物：「我今天吃了一顿抬杠」（= 午饭）
  - 当成动物：「路边有一只抬杠正在汪汪叫」（= 小狗）
  - 当成器官：「我的台账有点发炎」（= 挂号的理由）
  - 当成心情 / 地名 / 健身器材 / 任何实体
- **`why`** —— 每个选项都要有。这是真正的笑点位置，一句话收得干净，顺手讽刺「学黑话」这件事本身。范式：先认真指认（「一种小型犬」），再补一刀（「你要愿意，它明天还可以是一种心情」）。

写作要求：
- 至少 3 条 `wrong`（页面随机抽 3 条配 1 条正确）
- 误用的荒诞来自**认真的语气 + 完全不搭的释义**，不是来自夸张的修辞
- 正确答案要朴素到让人愣一下：「就这？」——对，就这，剩下三个才是黑话
- `why` 不超过两句，宁短不长

写完 `usage`，测验就自动出题，不用另外维护题库。

### 干扰项怎么编（decoys）

`decoys` 是留给外部消费者的备用题型（四选一猜释义），页面当前不用。每条词恰好写 **3 条**，标准是「**看起来完全可能是对的**」。

好干扰项的三个来源：

1. **挪用邻近真实技术** —— 把这个领域里另一个真东西写上去（Memory 的干扰项写 KV Cache）
2. **望文生义的合理推测** —— 一个没听过这词的人最可能猜的意思
3. **像模像样的生造** —— 用行业腔调编一个不存在但很像的机制

要避免：
- 明显搞笑的选项（一眼排除，题就废了）
- 和正确答案措辞结构差太远（长度、句式尽量接近）
- 其实也算对的模糊表述（必须是明确的错）

**判断标准**：把 4 个选项给一个懂行的人看，他要犹豫两秒。笑点应该来自「正确答案居然这么朴素」，而不是干扰项很蠢。

## 结构化输出

`npm run build` 产出两个文件：

### dist/lexicon.json ← 对外数据契约

```json
{
  "schema": { "version": 3, "fields": { "...": "每个字段的说明" } },
  "generatedAt": "2026-08-30T09:43:55.550Z",
  "count": 6,
  "categories": ["隐喻", "营销", "方法", "架构"],
  "terms": [
    {
      "slug": "taizhang",
      "term": "台账",
      "zh": "",
      "aliases": ["registry", "登记表", "账本"],
      "category": "隐喻",
      "kind": "代称",
      "added": "2026-08-30",
      "gloss": "一个小本子文件，记着…",
      "actually": "registry.json。换成英文文档里的说法就是…",
      "origin": "中文企业管理术语，出身工厂车间和财务科室…",
      "note": "给一个自动生成的 JSON 文件套上了会计科室的气味…",
      "decoys": ["…", "…", "…"],
      "sightings": [{ "date": "…", "source": "…", "quote": "…", "note": "…" }]
    }
  ]
}
```

保证：

- **纯字段，零 HTML** —— 每个字段都是干净文本，可任意渲染
- **字段恒定** —— 每条词都有全部 13 个 key，空值为 `""` 或 `[]`，不会缺 key
- **日期统一** `YYYY-MM-DD` 字符串
- **自带 schema** —— `schema.fields` 描述每个字段含义，读 JSON 就够了，不用回看本文档

拿它做自己的页面即可，无需解析 Markdown。

### dist/index.html ← 参考渲染

单文件、无依赖、双击可开。视觉为**复古街机图鉴**：奶油纸底 + 粗黑描边 + 硬阴影 + 丝网印半调网点。每个词是一张卡牌，每个分类有一只专属怪物立绘。

设计原则：**元素少而大**。一屏一张卡，不做细碎装饰堆叠——上一版的图钉 / 胶带 / 咖啡渍那种小元素全部撤掉了，凌乱感就是那么来的。

字段到版面的映射：

| 字段 | 版面位置 |
| --- | --- |
| `term` | 卡牌主词头（全卡最大的字） |
| `category` | 决定怪物立绘 + 左上「XX 系」徽章 + 稀有度星级 |
| `kind` | 右上词性徽章（蓝底 `术语` / 纸底 `代称`）+ 筛选栏第一排 |
| `zh` | 卡头 banner 中段 |
| `slug` | 图鉴编号 `NO.xxx`（稳定哈希）+ 副行 `/slug/` |
| `aliases` | 圆角标签行 |
| `sightings[0]` | **原话照录，全卡的钩子**——大字号引文打头，词条本体荧光高亮（金底 + 红划线），`note` 作为「译 →」紧跟其后。**不落来源和日期**——读者只认这句话本身。要的就是一开口就让人应激 |
| `gloss` | 「人话」气泡，紧跟原话后面做解释。**整个方框都是刮刮区**——涂层铺满气泡，拿金币光标刮开 |
| `actually` | 遭遇战结算的常驻内容；图鉴页只在词条没有目击记录时兜底出现。文本里的词条本体自动高亮，所以 `actually` **必须包含 term / zh / aliases 至少其一**（构建时会警告） |
| `origin` | 卡脚小字，只留「出身」一条 |
| `note` / `added` | 页面不展示，只留在数据里 |
| `sightings[1:]` | 页面不展示，只留在数据里 |
| `usage` | 遭遇战题目（不出现在图鉴页） |

两种模式：

- **图鉴** —— 一屏一张卡，`← / →` 键盘翻页（浏览器后退 / 鼠标侧键亦可回退），搜索（含别名与真身）、双排筛选（第一排按词性「该不该学」，第二排按分类「描述什么」）
- **遭遇战** —— 「谁没在瞎说」：怪物登场 + 血条 HUD，4 句用到这个词的话只有一句用对，其余三句风马牛不相及（讽刺向，不是真的要你学黑话）。答对 `HIT!` 怪物闪白，答错震屏 + `MISS!`。题量随词量自适应（至多 10 题）

分类与怪物对应：

| 分类 | 怪物 | 稀有度 |
|---|---|---|
| `方法` | `mon-fangfa` 流程图官僚 | ★★ |
| `架构` | `mon-jiagou` 摇摇欲坠积木塔 | ★★★ |
| `能力` | `mon-nengli` 秀肌肉大脑 | ★★★ |
| `隐喻` | `mon-yinyu` 半猫半茶壶 | ★★★★ |
| `营销` | `mon-yingxiao` 油腻推销员 | ★★★★★ |
| `失效` | `mon-shixiao` 融化崩坏体 | ★★ |

另有 `mascot-guide`（开场 + 结算的向导猫头鹰侦探）与 `boss-jargon`（多嘴九头蛇，分类缺图时的兜底）。

路由（hash 路由，单文件无后端依赖）：

| 地址 | 落点 |
|---|---|
| `#/` 或无 hash | 第一张卡 |
| `#/dict/{slug}` | 直达某张卡（如 `#/dict/taigang`），可分享、可刷新、可后退 |
| `#/quiz` | 直接开打 |

翻页推入浏览器历史，筛选（搜索/分类）只替换当前历史项，不会每敲一个字就污染后退栈。

## 部署

`dist/index.html` 是**完全自包含的单文件**——CSS、JS、全部 AI 素材（base64）都内联其中，零外部请求。部署 = 把这一个文件扔到任何静态托管：

- 任意 nginx / OSS / COS / GitHub Pages / Vercel 静态站：上传 `dist/index.html` 即可
- hash 路由不依赖服务端 rewrite，什么静态托管都能跑
- 想自定义域名或走私有托管：整个 `dist/` 目录直接当静态根即可，没有构建期外部依赖

`dist/lexicon.json` 是独立的数据契约文件，部署与否随意，页面运行不依赖它。

## 投稿入口

页面的常驻「投词」入口（header 金币按钮 + 遭遇战结算 CTA）只做一件事：新标签打开收集表单。纯前端跳转，零后端零接口，部署方式不变。

链接配置在 `submit.config.json`——链接是数据不是代码，换渠道不用改代码：

```json
{
  "label": "投词",
  "hint": "撞见新黑话？把词丢过来",
  "url": "https://my.feishu.cn/share/base/shrcn6SFjWEfb8FtclBBHIbYnpf",
  "github": "https://github.com/alienzhou/ai-agent-jargon-bestiary/issues/new?template=submit-term.yml"
}
```

- `url` 优先——飞书多维表格的表单分享链接（国内访问稳定、海外可达、免登录、匿名可填）
- `url` 为空时按钮退到 `github`——Issue 表单（`.github/ISSUE_TEMPLATE/submit-term.yml`，程序员通道，GitHub 账号即贡献人），footer 同步带「GitHub 递词」
- 两者都空 → 不渲染入口，构建时警告

### 为什么是表单，且只有四个空

门槛压到最低：**只有「词」必填，其余全选填**。提交者只给原料、不替收录人做判断——表单描述写得很直白：「不用判断它是不是黑话、值不值得收——那是收录人的事」。分类（`category`）、词性（`kind`）这类策展判断不出现在表单里。

| 表单字段 | 必填 | 对应词条字段 |
|---|---|---|
| AI 黑话词汇 | ✓ | `term` |
| 原话 / 上下文 | | `sightings[].quote` |
| 吐槽 / 补充 | | `note` |
| 贡献人 | | 暂无对应字段，收录时酌情并入 `note` |

提交时间由平台自动记录。

### 落表与分拣

表单只是多维表格「黑话投稿」表的一个收单视图：每次提交 = 往表里追加一行，填表人互不可见、也改不了任何行（包括自己写的）。收录人持有两列私有分拣列（表单里不出现）：`状态`（单选：待处理 / 已收录 / 追加 sighting / 不收，默认「待处理」）、`处理备注`。日常动作：开表 → 分拣 → 照录原话建词条 → 标状态归档。

## 视觉素材

页面里的怪物立绘不是 CSS 画的，是 AI 文生图产物。**仓库不绑定任何具体的文生图服务**：
`gen-art.sh` 只负责把统一风格前缀（`scripts/style.txt`）和你的主体描述拼成完整 prompt，
出图交给谁由环境变量 `JARGON_IMAGE_CMD` 决定；不设它就走手动模式（打印 prompt，你贴进任意工具生成后存到指定路径）。管线：

```
scripts/gen-art.sh <name> "<主体描述>"   → assets/img/<name>.png       # 本地生成原图，不入 git
scripts/opt-art.sh <name>                → assets/img/opt/<name>.{webp,avif}
node build.mjs                           → base64 内联进 dist/index.html
```

仓库只提交压缩后的 `assets/img/opt/`（webp + avif 双份约 1MB），
构建时按 `monImg()` 的降级口径挑用得到的那些内联进单文件。

**加新素材、改风格、验收标准 → 见 [ART.md](./ART.md)。** 那份文档是素材侧的唯一事实源：统一 prompt、四色调色板、命名约定、验收清单、已知坑。别在这里另起一套。
